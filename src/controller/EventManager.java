package controller;

class EventManager {
    /*
    Optional helper class to manage events between components (e.g., notifying the View when a character moves).
    Useful for implementing an observer pattern.
    */
}
